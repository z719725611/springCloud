spring:
  application:
    name: ui

eureka:
  instance:
    non-secure-port: ${server.port:8081}
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8077}/eureka/